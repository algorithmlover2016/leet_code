## [DP solution]
```
accumulate the sum from C(n, 0) to C(n, n) with increasing order.
for each element, we can decide selecting it or not.
    Selecting is in the former iteration.
    Removing the selector is in the current iteration.
```
